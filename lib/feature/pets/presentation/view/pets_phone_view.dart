import 'package:flutter/material.dart';

class PetsPhoneView extends StatelessWidget {
  const PetsPhoneView({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Center(child: Text('pets page')),
      ],
    );
  }
}
